ARM CENSUS AND LIVE-CASE INPUTS of the corpus at harness/ActiveWakeControl.json
(21236 cases), unit #47 ActiveWakeControl, SECOND DISPATCH.

Supersedes the first dispatch's split pair (this file and harness.live_case_inputs.txt,
which are joined here because there is no longer a small "live" subset to report
separately). The first dispatch's numbers are kept in the table at the bottom.

HOW IT WAS TAKEN. ONE fprintf patched into the GENERATED test source,
immediately after `LocalVar_a.restart` is read from the case stream and before
any call, then rebuilt in place and run:

  fprintf(stderr, "PROBE %d mode=%d nmodes=%d numbl=%d restart=%d harm0=%d "
          "time=%.17g freq0=%.17g phaseoff=%.17g minpit=%.17g maxpit=%.17g "
          "g0=%.17g g1=%.17g cplx0re=%.17g cplx0im=%.17g dt=%.17g\n",
          c, CntrPar_a.AWC_Mode, ...);

The probe reports INPUTS ONLY, which the case file fixes, so it needed no clean
tree and no reset window was spent on it. The generated source was restored from
a copy afterwards and the binary rebuilt and re-run: 21236 checked, 0 failed.

==============================================================================
CntrPar%AWC_Mode over all 21236 cases

    3546  mode=3          the base, values[0] in harness/ranges.toml
    3539  mode=0          no arm
    3538  mode=5
    3538  mode=1
    3538  mode=2
    3537  mode=4

  17697 of 21236 cases reach an arm -- 83%.

That even spread is the whole of the corpus repair. The first dispatch had 3032
of 3231 cases at mode = -600 and 91 reaching an arm at all. AWC_Mode is now
written in harness/ranges.toml as the enumeration it is, so R2's flag_variants
re-runs every ladder rung and every R6 magnitude under every mode.

==============================================================================
LocalVar%NumBl over the 17697 live cases

    17163  numbl=1        the base, int(3 * 0.35)
      266  numbl=2
      257  numbl=0
       11  numbl=3        <- and FIVE of these are at AWC_Mode == 1

`NumBl == 3` reaches an arm for the first time in this campaign. It arrives only
through the random-fill stratum (`_random_over` draws `rng.randint(lo, hi)` on
the stated [0, 3]), and R2 gives that stratum eight cases under EVERY declared
mode -- which is why five of the eleven are in Mode 1. Those five are what
killed the first dispatch's eleven blade-3 survivors: `AWC_angle(3)`, `phi3`,
`AWC_complexangle(3)` and `PitCom(3)` are now read as well as written.

==============================================================================
CntrPar%AWC_harmonic(1) -- the arm NOTHING reaches, still

    0 cases of 21236 have AWC_harmonic(1) == 0.

Unchanged, and unchangeable from harness/ranges.toml: AWC_harmonic is an
ALLOCATABLE INTEGER array, `_fill_array` gives it one monotone ramp, and any
bound that puts 0 into the array puts it at element 1 in EVERY case -- which
would delete the ELSE arms all 27 gate scenarios do exercise. Five mutation
survivors are here (R2 of mutation.census.txt); the cause is a regex in
`predicate_knobs_from` and it is escalated in DECISIONS.md.

The arm is NOT dead code: negating either guard moves 6309 and 6972 of 21236
cases in the negate_cond mutation run. What no input reaches is the guard being
true.

==============================================================================
CntrPar%AWC_phaseoffset over the 17697 live cases

    130 distinct values;  48 cases at exactly 0.0

The first dispatch had ONE value, 0.0, in all 91 live cases -- `_base`'s midpoint
of a +/-1e3 range is exactly zero, and at zero `x*D2R`, `x` and `x/D2R` are the
same bits. Held to one turn, [0, 360], the midpoint is 180. Four mutation
survivors died on this alone.

==============================================================================
CntrPar%PC_MinPit / PC_MaxPit -- the controller saturation window

    537 distinct (min, max) pairs over the live cases
    311 live cases with a window wider than 1e6

The first dispatch had ONE pair, (0, 600), in all 91 live cases, against gains
and errors three orders of magnitude wider -- so both callees clamped in every
case and which array element supplied kp or ki could not be seen. Three of that
group's five survivors are now dead. The two that remain are not about the
window; see R4 of mutation.census.txt.

==============================================================================
LocalVar%AWC_complexangle(1) AS SUPPLIED

    (0.0, 0.0) in ALL 21236 cases.

Unchanged. The COMPLEX(DbKi) view field crosses in both directions and the
generated test source contains ZERO `VITCMP("LocalVar.AWC_complexangle` lines --
481 VITCMP lines in total, three of them on LocalVar.PitCom and one on
DebugVar.axisTilt_1P, and none on this field. Five mutation survivors are here
(R1 of mutation.census.txt). A generator gap, escalated in DECISIONS.md.

==============================================================================
CASE ORDER, which is what decides a SAVE local's initialiser

    FIRST case of the stream          case 0:  mode 3, Time = 600
    first Mode 3 or 4 case            case 0
    first Mode 5 case                 case 20: Time = 600
    Mode 3/4 cases with Time <= 0     740 of 7083 -- all of them AFTER case 0

`Time = 600` is `_base`'s value on the defaulted +/-1e3 range at frac 0.8. It is
POSITIVE, so the very first live case takes `Time > StartTime` (StartTime is 0.0
and stays 0.0, because the only assignment to it is inside the
`AWC_harmonic(1) == 0` arm nothing reaches). The loop therefore runs and writes
`Error(1)` before `:289 DebugVar%axisYaw_2P = Error(1)` reads it, and the
initialiser is masked from then on.

THAT IS THE EXACT MECHANISM OF THIS DISPATCH'S ONE REGRESSION, ced1c24e, and it
is measured here rather than argued: 740 Mode 3/4 cases DO have `Time <= 0`, and
every one of them comes after case 0 has already written the slot.

==============================================================================
FIRST DISPATCH -> SECOND, side by side

  cases                                 3231   ->   21236
  cases reaching an arm             91 (2.8%)  ->   17697 (83%)
  no-op red test                    86 of 3231 ->   17645 of 21236
  post-integration red test         70 of 3231 ->   17521 of 21236
  distinct AWC_phaseoffset (live)          1   ->   130
  distinct (PC_MinPit, PC_MaxPit) (live)   1   ->   537
  live cases with NumBl == 3               0   ->   11
  cases with AWC_harmonic(1) == 0          0   ->   0
  AWC_complexangle supplied non-zero       0   ->   0
  mutation score                      0.7857   ->   0.9200
