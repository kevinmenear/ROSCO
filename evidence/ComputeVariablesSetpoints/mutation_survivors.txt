MUTATION: 1.0000, ZERO OPEN SURVIVORS -- ONE ANSWER PER MUTANT, AND EACH ANSWER
IS CHECKABLE WITHOUT RE-DERIVING IT.

`vit_mutate.py`, six token operators plus the call operators, run under
`scripts/mutate_guarded.sh` on the CLEAN tree against the same 22,397-case
corpus the green in `harness/ComputeVariablesSetpoints.json` was taken on.

    81 mutants, 0 nocompile
    71 killed of 71 scoreable          score 1.0000
     6 declared EQUIVALENT  (a claim about the PROGRAMS)
     4 declared UNREACHABLE (a claim about the CORPUS, each with evidence)
     0 open

    declared_but_killed  EMPTY        unreachable_but_killed  EMPTY

SIX SWEEPS, AND EVERY STEP IS A MEASUREMENT OF THE CORPUS RATHER THAN OF THE
TRANSLATION:

    0.7161   as generated, nothing declared
    0.7342   + the two dead-store equivalences
    0.7973   + seven degenerate-range pins and `iStatus = 0`
    0.8354   + `CntrPar_VS_RefSpd` UNPINNED again -- keeps both Region-3 arms
    0.8800   + four more equivalences, from the recovered id-to-site map
    0.8933   + `LocalVar_WE_Vw` pinned to the physical [3, 25] band
    1.0000   + `CntrPar_VS_MinOMSpd` at 0.0, and four unreachable declarations

===============================================================================
THE ID-TO-SITE MAP, WHICH IS WHAT MADE THE LAST HALF POSSIBLE

`vit_mutate.py` calls `mutants(args.unit.lower(), original)`. So
`cppmutate._mid` reproduces the sweep's ids under the LOWERCASED unit name and
not under the plan's spelling -- `_mid('ComputeVariablesSetpoints', ...)`
reproduces nothing, `_mid('computevariablessetpoints', ...)` reproduces all 81.
One character, and without it a `const_tweak` survivor printed as
`'0.0' -> '1.0'` cannot be attributed to any of this translation's four `0.0`
literals. Recover the map with:

    from harness.cppmutate import mutants
    for m in mutants(unit.lower(), open(cpp).read()):
        print(m.mid, m.operator, m.before, '->', m.after, 'L%d' % m.line)

===============================================================================
SIX EQUIVALENCES -- CLAIMS ABOUT THE PROGRAMS

  312f3565  L293  drop_factor  'VS_RefSpd_TSR * PRC_R_Speed' -> 'VS_RefSpd_TSR'
  f8e46a25  L293  arith_op     the same expression, '*' -> '/'

Both sit on the reference's own DEAD STORE, clean source
`ControllerBlocks.f90:148`:

    LocalVar%VS_RefSpd = LocalVar%VS_RefSpd_TSR * LocalVar%PRC_R_Speed
    LocalVar%VS_RefSpd = LPFilter(LocalVar%VS_RefSpd_TSR, ...)

The second statement OVERWRITES the first and reads `VS_RefSpd_TSR`, not
`VS_RefSpd`; there is no statement between them and `LPFilter`'s seven actual
arguments do not include `VS_RefSpd`.

**THE CONTROL IS INSIDE THE SWEEP.** The SAME product one screen down is
`saturate`'s upper bound at `:162`, it IS read, and its two mutants
(`09894fe5`, `be35d649`) are NOT declared -- they were KILLED, once the corpus
could reach them. Two identical edits to two identical expressions, one pair
declared and one pair killed, and the difference is dataflow.

  c303585e  L122  const_tweak  first  lpfilter_c `InitialValue` 0.0 -> 1.0
  642f565d  L271  const_tweak  second lpfilter_c `InitialValue` 0.0 -> 1.0

The literal beside each is `has_InitialValue = 0`, and BOTH crossing paths
branch on it: the generated Fortran bridge calls `LPFilter` WITHOUT the
optional argument (`bridge_result = REAL(LPFilter(..., inst), C_DOUBLE)`), and
the integrated `lpfilter.cpp` passes the flag through to the same test. The
value is never read on either path.

**THE CONTROL, again inside the sweep**: the neighbouring `has_InitialValue`
literal itself (`860bdd9e`, L122) is NOT declared -- it was killed, by pinning
`WE_Vw` off zero -- and the second call's copy of it was killed all along.

  9b32d4a5  L122  const_tweak  first  `restart ? 1 : 0` -> `? 2 : 0`
  30056504  L270  const_tweak  second `restart ? 1 : 0` -> `? 2 : 0`

`LPFilter`'s `reset` is a Fortran `LOGICAL(4)`. The generated bridge converts
with `(reset /= 0)` and the integrated C++ tests `(reset != 0)`, so 1 and 2 are
the same `.TRUE.` on both paths.

===============================================================================
FOUR UNREACHABLE -- CLAIMS ABOUT THE CORPUS, MEASURED

Full reasons and evidence are in `mutation/ComputeVariablesSetpoints.unreachable.json`
and the measurement is `evidence/ComputeVariablesSetpoints/negzero_census.txt`.

  f977278e  L145  `IF (SS_DelOmegaF < 0)` -> `<= 0`
      The two arms are `PC_RefSpd_PRC - SS_DelOmegaF` and `PC_RefSpd_PRC`,
      equal for every finite value AND for a NaN, because `x - 0.0 == x`
      including the sign of the zero. The ONLY discriminating input is BOTH
      `SS_DelOmegaF` and `PC_RefSpd_PRC` at `-0.0`. MEASURED: 2 cases have the
      first, 2 have the second, and **0 have both**. That is `generate.py`'s
      rule and not this corpus's luck -- R6 sets 40 scalar reals to negative
      zero ONE PER CASE, everything else at its base draw.

  f647c7b4  L327  `IF (SS_DelOmegaF > 0)` -> `>= 0`
      **AND THIS ONE REFUTED ITS OWN FIRST EXPLANATION.** The coincidence it
      needs IS in the corpus -- 1 case of 22,397 has `SS_DelOmegaF` and
      `VS_RefSpd` both at -0.0. It survives anyway because the unit's LAST
      statement erases the difference: the mutant computes
      `-0.0 - (-0.0) = +0.0`, and `MAX(-0.0, 0.0)` and `MAX(+0.0, 0.0)` are
      both `+0.0`. It is erased by `CntrPar_VS_MinOMSpd = 0.0`, which is the
      pin that KILLED four others. A negative minimum operating speed would
      recover it and is outside what the reference's own documentation admits.
      Four mutants for one, stated rather than quietly taken.

  c3c9ad10  L122  first  `restart ? 1 : 0` -> `? 1 : 1`
  b1377f87  L270  second `restart ? 1 : 0` -> `? 1 : 1`
      These force `LPFilter`'s `reset` true. Its guard is
      `IF ((iStatus == 0) .OR. reset)`, and `iStatus` is **0 on all 22,397
      cases** (measured), so the left operand is already true and `reset` never
      decides. `iStatus` is pinned because WITHOUT it the reference has no
      answer on this unit's `PRC_Mode == 1` path -- `interp1d` never assigns
      its own result at a NaN abscissa, and the harness fails 577 of 22,434
      (`harness.PINNED-CORPUS-RED.json`). So the only inputs that discriminate
      these two are inputs on which the reference is undefined.

===============================================================================
WHAT 1.0000 STILL DOES NOT COVER, STATED PLAINLY

  * The four declarations above are the CORPUS's limits, not the program's.
    Two of them (`c3c9ad10`, `b1377f87`) would be reachable on a corpus that
    could tell an undefined `interp1d` result from a defined one; the campaign
    has no `no_oracle_when` entry for that today and this unit did not add one.
  * `81` is the mutant population `cppmutate` offers on this translation. The
    sweep's own closing line says it: "no surviving mutant among those tried --
    which is not the same as no blind spot."
  * The GATE is blind to four of this unit's statements outright (both
    alternative torque-control laws and both Region-3 `VS_FBP` lookups have
    zero hits in all 27 scenarios) and to a fifth arm through one scenario
    only. Only the differential harness reaches them, and it now does so on a
    corpus that no longer divides by zero there.
