REVCHECK -- ONE FINDING, REPORTED RATHER THAN FIXED, AND THE CAUSE IS EXACT.

    $ python3 scripts/revcheck.py --unit ComputeVariablesSetpoints
    revcheck ComputeVariablesSetpoints: examined 1 unit, scanned 9 result artifact(s)
        41d383f-dirty      gate/ComputeVariablesSetpoints.json
        41d383f-dirty      gate/ComputeVariablesSetpoints.redtest.dead_power_tsr.json
        41d383f-dirty      gate/ComputeVariablesSetpoints.redtest.else_arm.json
        41d383f-dirty      gate/ComputeVariablesSetpoints.redtest.pc_spderr.json
        41d383f-dirty      gate/ComputeVariablesSetpoints.redtest.vs_refspd_forced.json
        41d383f-nogit      harness/ComputeVariablesSetpoints.json
        41d383f-dirty      harness/ComputeVariablesSetpoints.postintegration.copyback-redtest.json
        41d383f-dirty      harness/ComputeVariablesSetpoints.postintegration.json
        41d383f-nogit      mutation/ComputeVariablesSetpoints.json
      FINDING: DIRTY TREE
      1 finding(s)

WHAT IT IS NOT. It is not a SPLIT and it is not a MISSING loop_rev: all nine
artifacts name the SAME instrument revision, 41d383f, which is
`translation-loop` HEAD ("budget: the counter counts DISPATCHES...",
2026-08-20 07:42:41 -0400). The `-nogit` suffix on two of them is the
container, which carries no `git`; the `-dirty` suffix on the other seven is
the host seeing a non-empty `git status --porcelain`.

WHAT THE DIRT IS, measured rather than assumed:

    $ git -C translation-loop status --short
    ?? scripts/make_harness_guide.py

ONE UNTRACKED FILE, and NOTHING IMPORTS IT:

    $ grep -rln 'make_harness_guide' --include=*.py .  (excluding itself)
    importers: 0

It is a documentation generator (its own docstring says it produces
`HARNESS_AND_JUDGING.md` from a study of 61 unit sessions), it was already
present when this dispatch began, and it is on no code path that
`gate.py`, `vit_harness.py` or `vit_mutate.py` executes. So no artifact above
can have been influenced by it, and the instrument identity of all nine is
41d383f.

WHY IT WAS NOT FIXED HERE, stated so the next dispatch does not re-derive it.
Two repairs exist and both are campaign-level rather than this unit's:

  * COMMIT IT. That changes `translation-loop` HEAD, so any artifact re-taken
    afterwards would carry a different rev from the nine above -- a SPLIT,
    which is the finding revcheck actually cares about. It would have to be
    followed by re-taking all nine, priced by the RUNBOOK at ~25 minutes for
    twenty artifacts.
  * MOVE IT ASIDE and re-take the nine at a clean 41d383f. Same re-take cost,
    and it displaces work another dispatch left there.

Neither is a decision this unit should take on its own, and this unit closes
`deferred` on P12 regardless, so the re-take would not change its disposition.
Raised in DECISIONS.md for the Driver.

===============================================================================
AND THE DONE-CONDITION READS THE SAME FACT AS P14.

    [ FAIL ] P14  artifact revisions agree
      dirty_rev:gate/ComputeVariablesSetpoints.json=41d383f-dirty,
      harness/ComputeVariablesSetpoints.postintegration.json=41d383f-dirty
      -- the recorded SHA does not name the code that ran

So the untracked file costs this unit one of fourteen predicates. It is worth
being exact about what that predicate is asserting and what it is not: P14 is
not saying the artifacts disagree with each other (they do not -- all nine name
41d383f) and not saying the rev is missing. It is saying the stamp is HEDGED,
because `41d383f-dirty` is a true statement about a tree that contained
something 41d383f does not.

A THIRD REPAIR EXISTS AND WAS PRICED AND NOT TAKEN: move the file aside, re-run
all nine artifacts at a clean 41d383f, move it back. That would make the stamp
exact without committing or losing anything -- five gate runs (~5 min), three
harness runs with one integration wrapper edited and reverted in the middle
(~5 min), one 250-second mutation sweep inside a reset/restore window, plus
commits. Call it 25 minutes and four state transitions between the clean and
integrated trees.

It was not taken because THIS UNIT CANNOT REACH `COMPLETE` REGARDLESS: P12 is
0.734 against a threshold of 1.0, so the re-take would move the reading from
12 of 14 to 13 of 14 and change no disposition, while each of the four tree
transitions is a chance to leave the tree in the wrong state at the end of a
dispatch. The next dispatch of this unit will have to re-take the harness and
the mutation artifacts anyway, to score the corpus repair; the right moment to
make the rev exact is then, and the campaign-level question in DECISIONS.md
should be answered first.


===============================================================================
RESOLVED, AND NOT BY THIS UNIT.

Later in the same dispatch `translation-loop`'s working tree came back CLEAN --
`scripts/make_harness_guide.py` is no longer there, removed by something
outside this session while it ran. So the third repair became free: every
host-run artifact was RE-TAKEN with the instrument tree clean, and all five
gate artifacts and both post-integration artifacts now carry `41d383f` with no
suffix.

    green            5,252,000 / 0            (first take 5,252,000 / 0)
    pc_spderr          728,340                 (first take   728,340)
    else_arm                 0                 (first take         0)
    dead_power_tsr           0                 (first take         0)
    vs_refspd_forced 1,977,826                 (first take 1,977,826)

Every number identical, and the re-taken artifacts carry the ORIGINAL `notes`
as well as a line saying they are a re-take -- unit #57's rule that a re-take
must say at least what it overwrote. The two `-nogit` stamps that remain are
the container, which carries no `git`; P14 does not read those as dirty.
