// LIVENESS STUB. Reads no argument and writes no byte of the result.
// If the kernel PASSES this, it is not comparing anything this unit produces.
void Int2LStr(int Num, char* Int2LStr_result) {
    (void)Num;
    (void)Int2LStr_result;
}
