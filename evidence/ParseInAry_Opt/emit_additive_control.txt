emit.py BEFORE: 8414 bytes   AFTER: 8414 bytes
byte-identical with compare_record absent: True
with compare_record ON the source differs: True
  and it carries the capture: True / the counter: True

HOW THIS WAS RUN, 2026-08-19, sixth dispatch:

  git -C ../translation-loop show HEAD~1:harness/emit.py > /tmp/emitctl/emit_old.py
  docker cp /tmp/emitctl/ctl.py vit-dev:/tmp/emitctl/ctl.py
  docker exec vit-dev bash -lc 'cd /workspace/translation-loop && python3 /tmp/emitctl/ctl.py'

HEAD~1 is 74c2a32, the revision every artifact of this unit named before this
dispatch; HEAD is 6ec37f9, the revision they all name after it. The spec is the
loop's own `tests/test_emit_decomposed.py::spec()` -- a unit with a decomposed
struct argument and no record -- so the comparison is over a real generated
program and not a stub.

WHY IT IS BOTH HALVES. "Byte-identical when the flag is absent" alone would be
satisfied by a flag that does nothing at all, and this campaign has already
recorded an ablated rule that reported itself as one with no site (RUNBOOK,
unit #48). So the control also asserts that the source DOES change when the flag
is on, and that what it gains is the capture and its counter.
