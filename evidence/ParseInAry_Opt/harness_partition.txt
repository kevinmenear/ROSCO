ParseInAry_Opt -- the differential harness partitioned by (alloc_Ary on entry, arm)
MEASURED ON THE FOURTH DISPATCH'S CORPUS (19,536 cases), at loop 8c17719. The
third dispatch's table (17,520 cases), the second's (15,504) and the first's
(13,674) are in the git history of this file.

RE-TAKEN BY THE SCRIPT, `evidence/ParseInAry_Opt/run_partition_probe.sh`. The
corpus has now moved 13,674 -> 15,504 -> 17,520 -> 19,536, and every red-test
prediction below is read out of THIS run rather than scaled from the last one.

WHAT THE TABLE PREDICTED, AND WHAT THE RUNS MEASURED. Three of the four stub red
tests and the wrapper red test are predicted from these cells BEFORE they are
run:

  the whole unit as a no-op        19,276 = 19,536 - (184 + 76)   PREDICTED, EXACT
  the .NOT. AllowDefault_ arm       2,273 = 1,322 + 951           MEASURED 2,377
  the list-directed READ            1,044 =   233 + 811           MEASURED 1,150
  the IF (AryLen < 1) arm              19, unpredicted and unchanged from the
                                       third dispatch
  the deleted scalar copy-back     11,121, the aviFAIL-changed column summed

THE TWO MISSES ARE THE SAME 104-CASE CELL SEEN TWICE, AND IT IS IN THE TABLE.
`alloc = 0, other` carries 8,259 cases of which 104 CHANGED aviFAIL -- an arm
that set the flag and whose returned ErrMsg matches none of the classifier's
substrings, which is what R13's staging-capacity block produces: the bridge
REFUSES an ErrMsg longer than the buffer the case supplied and the reference
comes back with the message it arrived with. 2,273 + 104 = 2,377 exactly. The
READ residual is 106 and is NOT that cell: it is measured separately in
`read_residual.txt` and splits 88 + 18 rather than 104 + 2.

19536 case(s) with a probe record

  alloc  arm                 cases   aviFAIL-changed
      0  entered-failed         184         0
      0  not-allowed           1322      1322
      0  other                 8259       104
      0  read-failed            233       233
      1  already-alloc         7700      7700
      1  entered-failed          76         0
      1  not-allowed            951       951
      1  read-failed            811       811

  cases where the reference CHANGED aviFAIL: 11121 of 19536
  distinct returned ErrStat values: [0]
  distinct returned size_avcMSG:    [0]
