ParseInAry_Opt -- EVERY MUTANT ANSWERED, sixth dispatch, loop 6ec37f9
=====================================================================

153 mutants, 1 nocompile, 152 behavioural:

   128  KILLED by the differential harness (19,536 cases, sanitised sweep,
        AND THE RECORD THE CALL WRITES TO stdout compared for the first time)
    17  declared EQUIVALENT      mutation/ParseInAry_Opt.equivalences.{json,md}
     6  declared UNREACHABLE     mutation/ParseInAry_Opt.unreachable.json
     1  SURVIVED                 07b5ee72, and it has a row below
   ---
   152      score 128 / (152 - 17 - 6) = 128 / 129 = 0.9922

`declared_but_killed` and `unreachable_but_killed` are both EMPTY in
`mutation/ParseInAry_Opt.json`. That is a stronger statement this dispatch than
last: the oracle got WIDER and not one of the 23 declarations was refuted by it.

WHAT MOVED, AND IT IS ONE THING.

    fifth dispatch    19,536 cases  123 killed  17 eq  6 unr  0.9535  6 survivors
    sixth dispatch    19,536 cases  128 killed  17 eq  6 unr  0.9922  1 survivor

THE CORPUS DID NOT MOVE. `parseinary_opt_cases.bin` hashes
417c205656d2dd3aca728380163d1991d871b34ed76e776fd7f8f401d0089fc3 before and
after, 8,810,481 bytes, the same file the fifth dispatch scored. The five kills
are not new inputs. They are an OUTPUT that was never compared.


THE FIVE THAT DIED -- REGION C, AND IT IS CLOSED
================================================

    0562d216  const_tweak  L418  the field width 12 -> 13
    bb16f729  const_tweak  L423  the record's leading blank, 1 -> 2
    72edee9b  const_tweak  L427  the element loop's start, 0 -> 1
    39d15f1e  const_tweak  L438  the repaired guard's own constant, 0 -> 1
    4ca74dd3  negate_cond  L438  `if (n > 0)` negated -- the repair itself

All five edit `print_default_warning`, the C++ counterpart of
ROSCO_Helpers.f90:807. Five dispatches of this unit's evidence could not see
them, and the reason was structural rather than an oversight: the record crosses
no dummy argument, so `R4_compare_all_outputs` never named it, no `no_oracle`
could have excluded it -- it was never in the set -- and `vit_mutate.py` reads a
JSON payload out of `./test`'s stdout PRECISELY BECAUSE the reference may print,
so both sides' records were discarded before the comparison happened.

**THE FIX IS THE INSTRUMENT, NOT THE RECORD.** `harness/ranges.toml` now carries

    vit_record = { compare_record = "<why this stream is this unit's contract>" }

and `emit.py` captures fd 1 around EACH of the two calls separately, so the
translation's record and the reference's can be attributed and compared
bytewise. It is opt-in per unit, because whether a `PRINT` is part of a
contract or a debug line is not decidable from the source.

WHAT THE GREEN IS WORTH, AND IT IS THE NON-EMPTY COUNT. Two empty records
compare equal, so a corpus that never reached the printing arm would report
perfect agreement about nothing; `vit_harness.py` refuses to write an artifact
in which no case wrote a record. **15,744 of the 19,536 cases write one**, and
the translation reproduces gfortran's record on every one of them -- against 34
records for the side instrument that used to be the only thing looking.

RED-TESTED, AND THE COUNT WAS PREDICTED FROM THAT SAME NUMBER BEFORE THE RUN.
`evidence/ParseInAry_Opt/parseinary_opt.record-blank-stub.cpp` makes the edit
`bb16f729` makes -- the leading blank, 1 -> 2 -- and
`harness.record-blank-stub.json` reports **15,744 failed of 19,536**: exactly
the cases that write a record, and no others.

AND IT IS ADDITIVE, WHICH IS ALSO MEASURED. The four standing stub red tests
were re-taken with the record compared and came back **19,276 / 2,377 / 1,150 /
19** -- unchanged to the case. The new comparison moved no existing red.

The side instrument that used to carry these five (`print_replay.txt`, 34
records) is kept and is now a CONTROL rather than the evidence: it and the
harness agree about all five. Nothing is folded from it into the score, and
nothing needs to be.


REGION E -- THE ONE MUTANT LEFT, AND IT IS A CORPUS GAP WITH A MEASURED FIX
===========================================================================

    07b5ee72  negate_cond  L258  `if (is_eol(rec[p]))` negated, inside
                                 `eat_separator`'s blank scan

The mutant makes a BLANK look like an end-of-line, so `crossed_eol` is set where
the shipped program leaves it clear. `crossed_eol` is read at exactly one place:

    270:            sep = crossed_eol ? Sep::Eol : Sep::Blank;
    298:            if (first_item || sep != Sep::Blank) { return 5010; }

so the only records that separate the two programs are records in which a `;`
starts a NON-FIRST item and was reached across blanks. On those the shipped
program treats the `;` as LEGAL -- a NULL VALUE, which leaves its element of
`Ary` UNCHANGED -- and the mutant returns 5010.

**IT IS NOT (a) EQUIVALENT.** The two programs genuinely differ, and the
difference was measured over 22 records at the third dispatch
(`survivor_record_search.txt`).

**IT IS NOT (c) EITHER, AND THAT IS THIS DISPATCH'S CORRECTION.** The fourth and
fifth dispatches recorded it as a blind spot on the ground that "no
`no_oracle_when` can name" a record on which the reference returns success with
an undefined element. That is true of the arm the corpus takes and FALSE of the
subroutine: **there is a second arm on which the same record has a perfect
oracle**, and this dispatch measured it rather than argued it.

`evidence/ParseInAry_Opt/null_item_oracle_probe.{f90,txt}`, run against
gfortran:

    on entry           Ary = 7 8 9
    ALLOCATE STAT      = 5014      the allocation FAILED -- it was already allocated
    after the failure  Ary = 7 8 9  ALLOCATED = T
    record             = '1 ; 3 KEYNAME'
    READ IOSTAT        = 0         the reference calls this record LEGAL
    after the READ     Ary = 1 8 3  <- the null item KEPT the caller's own 8

    --- fresh ALLOCATE, same record
    after the READ     Ary = 1 49435 3  <- element 2 is UNDEFINED, not an oracle

So a `;` record on a case that hands the subroutine an **already-allocated**
`Ary` is fully defined on every output: the ALLOCATE fails (5014), the array
keeps the caller's values, the READ returns 0, and the element the null item
skips is the value the CASE supplied. The mutant on that same case returns 5010,
takes the READ-error arm, and writes a different `ErrVar%ErrMsg` -- which the
harness compares. **The kill needs no new judgement kind and no new oracle. It
needs one record shape the corpus does not contain.**

WHY IT WAS NOT TAKEN IN THIS WINDOW, stated as a refusal rather than left as an
absence. The record shape can only be stated in `generate.py` --
`harness/ranges.toml` cannot state a record SHAPE (§10.6) -- and a `generate.py`
change regenerates the corpus, which renumbers every case and invalidates all
23 result artifacts this dispatch has just taken, plus the partition, the read
residual, the line coverage and the six `unreachable` declarations derived from
it. That is a second full re-take, and it was begun too late in a 14,400-second
dispatch to finish. A named gap is worth more than a killed sweep.

THE NEXT DISPATCH'S WORK, IN ORDER, so it executes rather than re-derives:

  1. `harness/ranges.toml`: `alloc_Ary = { values = [1, 0] }`. This is a BASE
     DRAW correction of the kind unit #49 established and the one this unit
     already made twice; it puts R14's planted-record cases on the
     already-ALLOCATED arm, where the probe above says the oracle is total. It
     was measured once before (README §, "15,504 cases, ONE FAILING") and the
     one failure was a `/`-terminator record on a FRESHLY allocated `Ary` --
     i.e. the same undefined-element shape, on the arm this entry moves away
     from.
  2. `harness/generate.py`: a `_TAIL_VALUES` form carrying `;` in a non-first
     item position after blanks -- `1 ; 3`, and `1 ; ; 4` for the second guard.
     The fourth dispatch built every form there so that a successful read
     assigns every element it consumes (§10.5); this is the first form that
     deliberately does not, which is why step 1 has to come with it.
  3. Expect the six `unreachable` declarations to become REACHABLE. Withdraw
     them BEFORE the sweep -- a declaration the corpus reaches is
     `unreachable_but_killed`, and P12 fails the unit outright on it. They are
     the semicolon item (L298-L303) and the null repeat count (L338), and the
     same records reach both.
  4. Re-take all 23 artifacts, the partition, the read residual and the line
     coverage, in the order this dispatch used.

The denominator moves 129 -> 135 when those six are withdrawn, so the arithmetic
to beat is 135 / 135 and not 129 / 129. That is the honest way round: an
`unreachable` declaration is a debt against the corpus and not a credit, and
this pays the last six of them.


WHAT IS STILL NOT SEEN
======================

  * `Ary`'s undefined elements on a read that SUCCEEDS with the array freshly
    allocated -- 1,659 cases named and excluded by `no_oracle_when`, plus the
    `'/'` terminator path that no `no_oracle_when` can name. The `;` fix above
    does not close this; it ROUTES AROUND it, by putting the distinguishing
    record on the arm where the caller's own values are the oracle.
  * `AryLen` above 32, a stated narrowing against a reference defined to at
    least 100,000.
  * The `UnEc` echo WRITE, not translated: C++ cannot reach the Fortran unit
    table. Dead in all 27 scenarios and at every one of this unit's 230 calls.


TWO CONTROLS ON THIS DISPATCH'S NUMBERS
=======================================

THE SANITISED SWEEP STILL KILLS EVERYTHING THE VALUE ORACLE DOES. Computed from
the two committed artifacts rather than asserted:

    sanitised survivors                 1   ['07b5ee72']
    value-oracle survivors             18   07b5ee72 + 17 others
    sanitised survivors NOT in the value set   0   <- the superset holds
    only the sanitiser kills           17       (128 - 111)

`--sanitize` was worth 19 at the fifth dispatch and is worth 17 now. The two it
lost are not kills lost: they are two mutants the sanitiser used to be the ONLY
thing to kill, which the record comparison now kills on values as well. The
value oracle gained 7 and the sanitised sweep 5, and 7 - 5 = 2 is the same two
seen from the other side.

THE EMITTER CHANGE CANNOT HAVE TOUCHED ANOTHER UNIT.
`evidence/ParseInAry_Opt/emit_additive_control.{py,txt}` emits one spec through
emit.py at 74c2a32 and at 6ec37f9 with `compare_record` absent and compares the
generated C++ byte for byte: 8,414 bytes both, IDENTICAL. And the opposite half,
because a flag that does nothing would also pass the first: with the flag ON the
source differs and carries `vit_record.begin()` and `vit_record_nonempty`.
