ParseInAry_Opt -- the PRINT record, measured against the reference's own
runtime. THE INSTRUMENT FOUND A DEFECT ON ITS FIRST RUN AND THIS IS THE RUN
AFTER THE REPAIR.

FOURTH DISPATCH, loop 8c17719. 34 records built by `print_conformance.f90`,
each written by the reference's OWN list-directed PRINT statement (copied from
ROSCO_Helpers.f90 at 54dd134, not paraphrased) and replayed through the SHIPPED
`print_default_warning` by textual include.

WHAT THE FIRST RUN SAID, and it is in this file's git history at 0fed53ab:

    BASELINE: 34 replayed record(s), 1 mismatched
      case 24
        ref | ... Using default value of []|
        got | ... Using default value of [ ]|

gfortran writes NO separator blank before a CHARACTER item that follows an
EMPTY value list; the translation wrote `rec += ' '` unconditionally. The
sibling `ParseDbAry_Opt`'s translation guards the same blank with `if (n > 0)`
and its replay's baseline was 0 of 44, so this was one unit's transcription and
not a shared misreading of the runtime. **NO LAYER OF THIS UNIT'S EVIDENCE
COULD HAVE FOUND IT**: the differential harness compares out-parameters, the
gate's arm is dead in all 27 scenarios, and `vit_mutate.py` reads a JSON PAYLOAD
out of `./test`'s stdout PRECISELY BECAUSE the reference may PRINT, so both
sides' records are discarded before any comparison happens. The region no layer
compares is where the defect was.

THE REPAIR IS PRICED AND ITS COST IS IN THE SCORE. `if (n > 0)` adds two
mutants in a region the sweep cannot reach, and dropping one equivalence whose
mutant the 40-per-operator cap then excluded costs a third: 0.9685 -> 0.9535.
A correct program at a lower number.

AND THE FIVE SURVIVORS ARE MEASURED RATHER THAN ARGUED. All five PRINT mutants
move a record -- 25 to 34 of 34 -- so "not equivalent" is a measurement here and
not a claim. NOTHING FROM THIS RUN IS FOLDED INTO THE SCORE: a kill by a
different instrument is not a kill by the sweep, and `mutation/ParseInAry_Opt.json`
is not touched by this script. Entering them needs the `killed_by` declaration
kind the sibling escalated, and that is a loop change and an amendment rather
than this unit's to make.

THE NEGATIVE CONTROL IS BUILT INTO THE SHAPE: the replay enters the translation
only through `print_default_warning`, so the one surviving mutant OUTSIDE the
PRINT region must move nothing, and it does not.

34 reference record(s) from print_conformance.f90
CONFORMANCE -- the UNMUTATED translation against the reference:
BASELINE: 34 replayed record(s), 0 mismatched

PRINT-region survivors replayed: 5
  KILLED by the record comparison: 5
    0562d216  const_tweak    L418  '12' -> '13'   33 of 34 record(s) differ
    39d15f1e  const_tweak    L438  '0' -> '1'   25 of 34 record(s) differ
    4ca74dd3  negate_cond    L438  'if (n > 0)' -> 'if (!(n > 0))'   34 of 34 record(s) differ
    72edee9b  const_tweak    L427  '0' -> '1'   33 of 34 record(s) differ
    bb16f729  const_tweak    L423  '1' -> '2'   34 of 34 record(s) differ
  STILL ALIVE: 0

NEGATIVE CONTROL: 1 survivor(s) OUTSIDE the PRINT region, 0 of them moved a record
