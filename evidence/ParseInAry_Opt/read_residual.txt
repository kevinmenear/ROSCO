ParseInAry_Opt -- the 106 cases the no-READ stub fails that the partition's
`read-failed` cells do NOT predict, classified rather than explained.

FOURTH DISPATCH, 19,536-case corpus, loop 8c17719. The no-READ stub fails 1,150
and the two `read-failed` cells of `harness_partition.txt` predict 1,044 -- and
the measured count ON A read-failed ARM is 1,044 EXACTLY. The residual is 106
and it is two different things:

    88   the reference's READ SUCCEEDED and its aviFAIL is still the 300 the
         case supplied. Deleting the READ is visible in `Ary`, not in the error
         flag. These are the records R14's numeric leads and this dispatch's
         blank tails and tail values put in front of the key -- every record
         this campaign planted before b33a761 was LETTERS, so every matched
         record FAILED the read and the failing set was exactly the two error
         cells. The block runs 15,396 .. 16,098, which is inside R14.
    18   `already-alloc`: the reference had already set aviFAIL from the
         ALLOCATE arm, and deleting the READ removes the message that arm's
         ErrMsg is then overwritten by.

THE PREDICTION IS A PARAMETER NOW. It was a literal `634` written for the third
dispatch's corpus, and a re-run on a wider one printed that stale number beside
a live measurement -- the shape this file exists to remove. It is
`READ_FAILED_CELLS`, defaulting to this dispatch's 1,044.

1150 FAILING case(s) under the no-READ stub

  the REFERENCE's arm on each failing case
    already-alloc            18
    no-error                 88
    read-failed            1044

  PREDICTED (the two read-failed cells)      1044
  measured on a read-failed arm             1044
  RESIDUAL -- the reference's READ SUCCEEDED 88
  their returned aviFAIL values              [300]
  first ten case indices                     [15396, 15414, 15432, 15450, 15720, 15738, 15864, 15882, 16080, 16098]
