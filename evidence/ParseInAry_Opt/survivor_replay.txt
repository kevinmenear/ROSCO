ParseInAry_Opt -- EVERY SURVIVOR PUT THROUGH THE OTHER INSTRUMENT
second dispatch, on the corrected corpus (15,504 cases) and the 87 survivors
of mutation/ParseInAry_Opt.json.

THE RUNNER HAD A DEFECT AND IT IS THE REASON THIS FILE WAS RE-TAKEN.
`parser_conformance.cpp` reaches the translation by TEXTUAL INCLUDE, and the
compile line carried `-I rosco/controller/src` BEFORE the directory holding
the mutant. `vit integrate` puts a copy of the translation in that directory,
so on an integrated tree `#include "parseinary_opt.cpp"` resolved to the
SHIPPED file and no mutant was ever compiled. The first dispatch ran this
before integration, when that file did not exist, so it was right by accident.
Re-run as it stood, the report was:

  region        verdict      count
  READ          unreached    71
  other         unreached    16
  the parser replay KILLS 0 of 87 survivors

WHICH IS A PERFECT NEGATIVE CONTROL. The runner's only control was `a
survivor outside the parser must come back unreached`, and 87 of 87 unreached
satisfies it completely. A control that a broken instrument passes MORE easily
than a working one is not a control -- so the runner now carries a POSITIVE
one, taken before it scores anything: `;` added to `is_value_terminator`, a
perturbation whose delta this campaign has already measured through the same
path, must come back at 3 records of 113 or the run refuses.

  control:          the shipped translation replays 0 mismatched of 113
  positive control: ';' in is_value_terminator replays 3 of 113 (3 expected)

  region        verdict      count
  READ          KILLED       52
  READ          unreached    19
  other         KILLED       1
  other         unreached    15

the parser replay KILLS 53 of 87 survivors, 52 in the READ region and 1 outside it

THE NEGATIVE CONTROL STILL HOLDS, and it holds with one fewer member than the
first dispatch's 16 of 17: `66860b6f` (`assign_errmsg`, `>` -> `>=`) is no
longer a survivor at all -- the corrected base draw of R13's staging-capacity
block kills it in the differential harness. 15 of the 16 survivors outside the
parser come back unreached; the one that crosses is the one that should:

  b93bad70  line 194  const_tweak: '200000000' -> '200000001'
      killed by 1 record(s) of 113

`MaxRepeat` is file-scope, so it lands outside the READ span by position -- and
it is read by the parser and by nothing else. The record that kills it is the
one put in the corpus for exactly that boundary, MEASURED ('199999999*7' and
'200000000*7' give 7 7, '200000001*7' gives 5010) rather than recalled.

THE 19 READ SURVIVORS THE REPLAY ALSO CANNOT REACH:
  2f8001ae  line  202  compare_op: '<' -> '<='
  08ff2c98  line  209  compare_op: '<' -> '<='
  e6bc72cb  line  217  compare_op: '>' -> '>='
  d99e45ef  line  248  compare_op: '<' -> '<='
  da89c70f  line  257  compare_op: '<' -> '<='
  b663872a  line  263  compare_op: '<' -> '<='
  112d63b1  line  265  compare_op: '<' -> '<='
  58ec3d92  line  265  index_offset: '[p]' -> '[p + 1]'
  7fd4edf1  line  287  compare_op: '<' -> '<='
  78f9ef7a  line  287  index_offset: '[p]' -> '[p + 1]'
  4b65a2f1  line  303  compare_op: '<' -> '<='
  9a98ecd6  line  303  index_offset: '[p]' -> '[p + 1]'
  64cfaba1  line  316  compare_op: '<' -> '<='
  a4bdfbcc  line  319  compare_op: '>' -> '>='
  356f870c  line  319  compare_op: '<' -> '<='
  e905195c  line  326  arith_op: 'MaxRepeat + 1' -> 'MaxRepeat - 1'
  5477861c  line  326  const_tweak: '1' -> '2'
  b300704f  line  337  index_offset: '[p]' -> '[p + 1]'
  4f1acf76  line  346  const_tweak: '0' -> '1'

Fifteen of the nineteen are `p < len` -> `p <= len` or `rec[p]` -> `rec[p + 1]`
at the record-bound checks, reading ONE BYTE PAST a blank-padded buffer whose
next byte is, on this allocator, another blank. The answer does not change.
That is the campaign's sanitiser-build class (unit #31's open amendment); this
unit adds only that its 19 belong to it, measured.

THE SCORE IN mutation/ParseInAry_Opt.json IS UNCHANGED BY THIS RUN, at
69/156 = 0.4423, AND NOTHING IS DECLARED EQUIVALENT.
A kill by a different instrument on a different corpus is not a kill by this
one, and folding it in would make the number unreadable. What the run replaces
is the ARGUMENT.
