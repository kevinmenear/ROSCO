SUPERSEDED, AND KEPT FOR TWO REASONS -- ONE OF WHICH IS A DEFECT IN THIS
INSTRUMENT THAT THE SECOND DISPATCH FOUND.

1. ITS SUBJECT IS GONE. This searched a record space for a record distinguishing
   each of the 21 open mutation survivors. There are none: the second dispatch
   closed the unit at 144 / 144. The mutant ids below no longer denote the same
   sites either -- two repairs to the translation renumbered them, and a mutant
   id is an occurrence index.

2. PART OF ITS RECORD SPACE IS INADMISSIBLE TO THE UNIT, and no row says so.
   The space is "every string of length 1..3 ... at the HEAD of a 200-byte
   blank-padded record and again at its TAIL, so the token's last character is
   the record's last byte". **The unit can never be handed a TAIL record.**
   `GetWords` writes `Words(IW) = Line(Ch+1:Ch+NextWhite-1)` -- a word
   LEFT-JUSTIFIED in a blank-padded `CHARACTER(200)` -- so a record whose
   non-blank token sits at bytes 197-199 does not exist for this unit. Rows
   reported as `TAIL|...` therefore name records admissible to
   `list_read_reals` (the FUNCTION) and not to `ParseInput_Dbl_Opt` (the UNIT).

   THIS IS THE SAME CLASS OF ERROR THE FIRST DISPATCH ALREADY CORRECTED ONCE,
   and missed the second instance of. Section 6 of
   `mutation/ParseInput_Dbl_Opt.equivalences.md` records that two rows here were
   misfiled as corpus levers because `GetWords`' separator set means no byte of
   `Words` is ever a comma or a semicolon. The separator set was checked; the
   JUSTIFICATION was not. Both are properties of the same callee.

   The general form, and it is what belongs in a runbook rather than here: **a
   search that drives an internal function directly has a wider input space than
   the unit does, and the filter is the CALLEE that stands between them --
   every property of it, not the one that came to mind.**

   Where it mattered: `71dee475` (`q < len` -> `q <= len` in the IEEE-word copy
   loop) is reported below as distinguished by `TAIL|nan|...`, which the unit
   cannot be handed. It was closed for a different reason -- the repair that
   removed the `n < 62` bound made `q` able to reach `len` at all, and the
   `repword` corpus form then killed it.

The original bytes follow unedited.
========================================================================

ParseInput_Dbl_Opt -- a record that distinguishes each surviving mutant
from the SHIPPED translation, searched rather than argued.
Built with -fsanitize=address,undefined; the BASELINE ran clean under
those flags. The sanitiser is a GUARD here rather than the instrument:
byte 201 of Words is Words(2)(1:1), so a read one past the record is
inside the allocation and the difference it makes is a VALUE.

RECORD SPACE: 131312 records. Every string of length 1..3 over
  "0123456789+-.eEdDqQnNaAiIfFtTyYxX ,;/*()" at the HEAD of a 200-byte
  blank-padded record and again at its TAIL, so the token's last character
  is the record's last byte; plus 32 crafted full-width records. ONE
  element is read -- the reference transfers one -- pre-set to the
  -987.654 sentinel and printed as bits, so a stored 0.0 is
  distinguishable from an untouched slot. The record lives in the first
  200 bytes of a 400-byte object whose second half holds a parameter
  name, which is what Words(1) and Words(2) are.

WHAT A ROW MEANS. `differs` names the FIRST record on which the mutant's
(iostat, element bits) triple differs from the shipped translation's --
that record is the corpus lever and the mutant is NOT equivalent. `none`
means no record IN THIS SPACE distinguishes it, which is evidence toward
equivalence bounded by the space and is not a declaration.

16 of 23 non-PRINT survivors are distinguished by some record in this space (16 by a VALUE, 0 by an ADDRESS -- the sanitiser aborting on a read the shipped translation does not make).

209527b9  compare_op     L 227  '<' -> '<='
    NONE -- no record in this space distinguishes it
26cb52f1  compare_op     L 217  '<' -> '<='
    NONE -- no record in this space distinguishes it
32baae1c  index_offset   L 306  '[q]' -> '[q + 1]'
    DIFFERS on 587 record(s). First:
      HEAD|1*|0|c08edd3b645a1cac   vs   HEAD|1*|5010|c08edd3b645a1cac
4198da25  compare_op     L 214  '<' -> '<='
    NONE -- no record in this space distinguishes it
479d0b11  const_tweak    L  49  '200' -> '201'
    NONE -- no record in this space distinguishes it
4c451ef0  const_tweak    L 195  '62' -> '63'
    DIFFERS on 1 record(s). First:
      FULL|full:nan63|5010|c08edd3b645a1cac   vs   FULL|full:nan63|0|7ff8000000000000
4cbc3c66  index_offset   L 290  '[p]' -> '[p + 1]'
    DIFFERS on 6391 record(s). First:
      HEAD|,|0|c08edd3b645a1cac   vs   HEAD|,|5010|c08edd3b645a1cac
5cb7425d  index_offset   L 305  '[q]' -> '[q + 1]'
    DIFFERS on 507 record(s). First:
      HEAD|1*|0|c08edd3b645a1cac   vs   HEAD|1*|5010|c08edd3b645a1cac
5fed96af  index_offset   L 334  '[p]' -> '[p + 1]'
    DIFFERS on 520 record(s). First:
      HEAD|..|5010|c08edd3b645a1cac   vs   HEAD|..|5010|0000000000000000
6a6668b2  negate_cond    L 334  'if (p < len && !is_separator(rec[p]))' -> 'if (!(p < len && !is_separator(rec[p])))'
    DIFFERS on 2130 record(s). First:
      HEAD|.|5010|0000000000000000   vs   HEAD|.|5010|c08edd3b645a1cac
71dee475  compare_op     L 195  '<' -> '<='
    DIFFERS on 19 record(s). First:
      TAIL|nan|0|7ff8000000000000   vs   TAIL|nan|5010|c08edd3b645a1cac
72653030  index_offset   L 232  '[p]' -> '[p + 1]'
    DIFFERS on 310 record(s). First:
      HEAD|0+0|0|0000000000000000   vs   HEAD|0+0|5010|c08edd3b645a1cac
8d81269b  index_offset   L 298  '[p]' -> '[p + 1]'
    DIFFERS on 924 record(s). First:
      HEAD|0;|0|0000000000000000   vs   HEAD|0;|5010|c08edd3b645a1cac
8e796788  const_tweak    L 159  '0' -> '1'
    NONE -- no record in this space distinguishes it
97c42adb  compare_op     L 195  '<' -> '<='
    DIFFERS on 1 record(s). First:
      FULL|full:nan63|5010|c08edd3b645a1cac   vs   FULL|full:nan63|0|7ff8000000000000
9e00d730  index_offset   L 237  '[p]' -> '[p + 1]'
    DIFFERS on 221 record(s). First:
      HEAD|0+0|0|0000000000000000   vs   HEAD|0+0|5010|c08edd3b645a1cac
b03a94c5  index_offset   L 198  '[n]' -> '[n + 1]'
    DIFFERS on 36 record(s). First:
      HEAD|nan|0|7ff8000000000000   vs   HEAD|nan|5010|c08edd3b645a1cac
b8766137  index_offset   L 237  '[p]' -> '[p + 1]'
    DIFFERS on 220 record(s). First:
      HEAD|0-0|0|0000000000000000   vs   HEAD|0-0|5010|c08edd3b645a1cac
c06ced0a  const_tweak    L 240  '0' -> '1'
    DIFFERS on 152 record(s). First:
      HEAD|.+|5010|c08edd3b645a1cac   vs   HEAD|.+|5010|0000000000000000
c28ed6fb  index_offset   L 232  '[p]' -> '[p + 1]'
    DIFFERS on 308 record(s). First:
      HEAD|0-0|0|0000000000000000   vs   HEAD|0-0|5010|c08edd3b645a1cac
d7f942e4  const_tweak    L 211  '0' -> '1'
    DIFFERS on 546 record(s). First:
      HEAD|.|5010|0000000000000000   vs   HEAD|.|5010|c08edd3b645a1cac
eba1c07e  compare_op     L 237  '<' -> '<='
    NONE -- no record in this space distinguishes it
f13a04da  compare_op     L 213  '<' -> '<='
    NONE -- no record in this space distinguishes it
