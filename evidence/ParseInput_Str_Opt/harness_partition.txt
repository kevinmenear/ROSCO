harness partition -- 14116 case(s), corpus translations/ROSCO_Helpers/parseinput_str_opt_test/parseinput_str_opt_cases.bin

arm                            cases  scalar-changed   record  Variable moved  LEN(Variable) < 6
default-warned                 10595               0    10595           10595                672
not-allowed                     2123            2123        0               0                130
read-ok                         1117               0        0            1057                  6
pre-failed                       190               0        0               0                  0
not-allowed, msg refused          91              91        0               0                  0
TOTAL                          14116            2214    10595           11652                808

PREDICTIONS READ OFF THIS TABLE, each stated before its run:
  post-integration wrapper red test (the reverse copy deleted):
      the `scalar-changed` column summed = 2214 must FAIL
      the rest, 11902, write no scalar and must PASS
  the PRINT deleted:  the `record` column summed = 10595 must FAIL
  the `.NOT. AllowDefault_` arm stubbed out: the two `not-allowed`
      rows summed = 2214 must FAIL
  the READ deleted:   the `read-ok` row's `Variable moved` count = 1057 must FAIL

AND THE TWO COLUMNS THAT ARE MEASUREMENTS RATHER THAN PREDICTIONS:
  `read-failed` is DEAD IN THE PROGRAM -- an `A` edit descriptor
  whose width equals the item length, over an internal record that
  is blank-padded when short, has no failure mode. Its count here
  is a second instrument agreeing with record_form_probe.txt's
  144 of 144 IOSTAT zeros, on the corpus rather than on a form list.

  `LEN(Variable) < 6` counts the cases where `Variable = 'unused'`
  TRUNCATES, which is the ONLY input this unit's PRINT record has:
  every other case writes `...default value of unused`. A corpus
  with a zero in this column would make the record a constant and
  every mutant of the value item equivalent.
