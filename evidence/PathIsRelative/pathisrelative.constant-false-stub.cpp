// STUB, not a translation. Reads NO argument and returns the constant .FALSE. --
// the answer every one of the 27 scenarios produces at the instrumented call
// site (ReadSetParameters.f90:673, CntrPar%PerfFileName, an absolute path).
// If the kernel PASSES this, the kernel is a lookup table for this unit.
#include <cstdint>
int32_t PathIsRelative(char* GivenFil, int len_GivenFil) {
    (void)GivenFil; (void)len_GivenFil;
    return 0;
}
