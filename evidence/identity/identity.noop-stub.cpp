// STUB, not a translation. The INPUT to a measurement (X4), kept so the
// measurement can be re-taken. Reads no argument and writes no element of the
// result. If the kernel still passes, its comparison cannot see this unit at
// all.
void identity(int n, double* identity_result) {
    (void)n;
    (void)identity_result;
}
