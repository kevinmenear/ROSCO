// STUB, not the translation. Both wrapping branches DELETED -- the unit reduced
// to the ELSE arm. This is the "does the kernel reach the branch the unit exists
// for" stub of RUNBOOK unit #24, and coverage predicts it PASSES.
double wrap_180(double x) {
    return x;
}
