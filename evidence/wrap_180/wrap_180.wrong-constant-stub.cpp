// STUB, not the translation. A determinate, finite, WRONG constant -- the
// liveness stub. -7.25 is outside the captured domain [-4.4206, +1.5171], so no
// case can be right by accident (RUNBOOK unit #25: choose the constant against
// the call site's arguments, not against the type).
double wrap_180(double x) {
    (void)x;
    return -7.25;
}
