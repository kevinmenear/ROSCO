// STUB -- NOT the shipped translation. BOTH wrapping branches deleted -- the unit reduced to identity. This is the does-it-reach-the-branch stub (unit #24), not the liveness stub.
double wrap_360(double x) {
    return x;
}
