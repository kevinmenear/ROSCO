{
 "unit": "interp2d",
 "against": "integrated",
 "checked": 1147,
 "failed": 0,
 "inadmissible": 0,
 "rule_coverage": [
  "  applied  R5_non_degenerate_shape    5 varied extent(s) at [3, 3, 4, 4, 7] and [3, 4, 4, 5, 5]; TIED, so NOT pairwise distinct and a stride error that agrees when they are equal is unreachable here: n_zData_cols follows n_xData, n_zData_rows follows n_yData, NO index parameter identified, array elements distinct",
  "  N/A      R2_flag_values             no enumerated-value parameter -- no branch this rule can reach",
  "  applied  R6_reference_literals      100 value(s) from 9 literal(s) in the reference, each under every declared value of 0 flag(s); 123 integer value(s) -- the decade boundaries and the representable extremes of a 32-bit INTEGER, signed -- across the scalar integer parameter(s) whose range is defaulted rather than declared; 66 real magnitude(s) -- the thresholds at which a product first goes subnormal, first flushes to zero and first overflows, plus one decade inside each region -- across the scalar real parameter(s) whose range is defaulted rather than declared; 80 of those magnitudes re-run with every OTHER defaulted scalar real pinned to an isolating 0.0 or 1e300, because a rung on one parameter is drowned by the ordinary-magnitude terms the others put in the same expression; 20 of those magnitudes re-run UNPINNED, every other real at its ordinary default, because a coefficient that is a bare product is annihilated by both isolating values and has no term to remove; 2 scalar real(s) at NEGATIVE ZERO -- the value the ladders' own `dict.fromkeys` dedup absorbs into 0.0, and the only input on which a `< 0` saturation guard and a `<= 0` one produce different bits; 4 combination(s) of the 1 quantit(y/ies) the reference's own predicates test, the full cross product, each quantity taken WHERE IT ENTERS -- an element of an array parameter when the reference reassigns the tested name from one, because a ladder over that name is dead; 20 case(s) across 2 predicate(s) the reference writes between TWO VARIED QUANTITIES, each with one side set FROM the other -- at equality, where an ordering predicate changes its answer, under every knob combination, plus the two neighbouring representable values once for strictness; a ladder on either side alone cannot reach these, because whichever base value is larger is larger in every case; 114 case(s) across 4 predicate(s) the reference writes against a REDUCTION of a varied array (MAXVAL(xData), MAXVAL(yData), MINVAL(xData), MINVAL(yData)), the scalar set FROM that reduction of the body drawn in the SAME case, at the value itself and its two neighbouring representable numbers, over the ordinary body and its REVERSE so the extremum is once first and once last -- or, for an array stated `nondecreasing_only`, over the NON-DECREASING shapes (the adjacent equal pair at three positions and the constant array) so the extremum is once unique and once repeated; the other side is a FUNCTION of a parameter, so it has no name to pin and no ladder can reach the equality; 52 array ordering(s) -- reversed, one adjacent pair inverted at the first / an interior / the last position, an adjacent EQUAL pair, and the constant array, each at UNIT steps, again at steps spanning the parameter's admissible range, and again with each of 2 constant(s) the reference itself names as the adjacent DIFFERENCE, because an ordering at one step size varies the SIGN of a difference and never its MAGNITUDE -- across the array(s) the reference subscripts twice in one statement, directly or through a whole-array copy -- MINUS the reversed and pair-inverted shapes for xData, yData, stated `nondecreasing_only`, whose reference goes out of bounds on an inversion and not on a tie; 88 character value(s) from a corpus of 22 (the reference's own character literals and their collating neighbours) across 1 string(s), each at length(s) [1, 2, 7, 12] and in the mixed / padded / all-blank / leading-blank shapes, plus 132 planted at an interior position of an ordinary string so the character AFTER a matched literal is not its own collating neighbour",
  "  N/A      R8_file_contents           no file-valued input -- no reference OPENs or INQUIREs on a dummy",
  "  applied  R3_no_dead_signal          5 real value(s), none zero in every case",
  "  N/A      R11_one_at_a_time          no admissible baseline supplied -- there is no case here in which exactly one predicate is away from admissible, so a unit that writes one output from many sites is observable only at the last site that writes it",
  "  N/A      R12_narrowing_width        no CHARACTER local is narrower than a dummy assigned into it -- the reference truncates nothing, so there is no boundary for this rule to reach",
  "  applied  R13_staging_capacity       256 case(s) sweeping the stated staging capacity of every deferred-length CHARACTER output from 0 to 255 bytes above what the case supplies, every other input at its base draw -- so the output has one length across the block and exactly one case sizes the buffer TO it, one a byte short and one a byte long. A capacity a case does not state is `emit.ALLOC_HEADROOM`, which is what every case outside this block still carries",
  "  N/A      R14_planted_word           no CHARACTER ARRAY input -- there is no element for a scalar string to be a word OF, so no comparison of this shape exists to reach",
  "  applied  R1_direct_call             the unit is called directly; no scenario can fail to reach it",
  "  applied  R4_compare_all_outputs     return value + 5 out-parameter(s): ErrVar_size_avcMSG, ErrVar_aviFAIL, ErrVar_ErrStat, ErrVar_ErrMsg_n, ErrVar_ErrMsg"
 ],
 "mismatches": [],
 "measures": "the integration wrapper's marshalling ONLY. After integration the Fortran body IS the translation, so no harness against this build can compare arithmetic; both sides run the same code, and a failure means the wrapper corrupted an argument or an output on the way through. The arithmetic is covered by the pre-integration harness, the kernel and the gate.",
 "loop_rev": "eb5028e",
 "vit_rev": "a16a7ab"
}
