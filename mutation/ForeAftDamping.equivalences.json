{
 "_why": "ONE DECLARATION, AND IT IS (c)-CLASS -- behaviour-preserving on every input, not merely unreachable on this corpus. The survivor is the `1` of `LocalVar->restart ? 1 : 0` at translations/Controllers/foreaftdamping.cpp:115, the ninth argument of this unit's single `picontroller_c` call. `PIController`'s `reset` dummy is Fortran LOGICAL and crosses as INTEGER(C_INT); every converter on every path in this campaign folds non-zero to .TRUE.:\n\n    clean tree, the generated callee bridge this sweep scored against --\n      translations/Controllers/foreaftdamping_test/foreaftdamping_callees.f90:28\n        bridge_result = REAL(PIController(..., piP_f, (reset /= 0), inst), C_DOUBLE)\n\n    integrated tree, unit #33's translation, which IS picontroller_c afterwards --\n      translations/Controllers/picontroller.cpp:30\n        if (reset != 0) {\n\nso `? 2 : 0` and `? 1 : 0` are one argument and no input can separate them. The same declaration unit #50 made at flapcontrol.cpp:217 and unit #51 made at floatingfeedback.cpp:160 and :163.\n\nEXECUTED, NOT ARGUED (unit #48's rule), in evidence/ForeAftDamping/equivalence_probe.txt, over the SCORED corpus with the case file byte-identical either side (md5 58d86ec137a144e769513938e5c170d1, 7567 cases):\n\n    restart == 1 -- the `? 1` branch TAKEN     3776 of 7567   <- the positive control\n    restart == 0 -- the `: 0` branch taken     3791 of 7567\n\nThe call is guarded by nothing and runs on every case; 3,776 of them select the very token the mutation edits, and the mutant still survives. THE COUNTER-CONTROL that the operator is not blind here: the OTHER arm of the SAME conditional, `'0' -> '1'` on the SAME line (a9c89ca7), was KILLED at 3389. And a third number from the other side: the `I0` argument's `'0.0' -> '1.0'` (6a35df73) was killed at exactly 3776 -- PIController returns its I0 on precisely the reset path this token selects, so the two counts agreeing to the case is an arithmetic control on the census.\n\nNOTHING ELSE IS DECLARED. Both undeclared takes (mutation/ForeAftDamping.clean.first_take.json over the 2539-case corpus and .second_take.json over the 7567-case one) killed 8 of 9; this single mutant is the whole difference between 0.8889 and 1.0000, and `declared_but_killed` is empty on the re-take.",
 "7c03f696": "const_tweak '1' -> '2' at translations/Controllers/foreaftdamping.cpp:115 -- the `1` of `LocalVar->restart ? 1 : 0` in this unit's picontroller_c call. The bridge converts with `(reset /= 0)` and the integrated picontroller.cpp:30 with `if (reset != 0)`, so 2 and 1 are the same LOGICAL .TRUE. and the two programs are identical. Live site: 3776 of 7567 cases take that branch; counter-control: `'0' -> '1'` on the same conditional was killed at 3389."
}
