// C++ implementation of Fortran function: PreFilterMeasuredSignals
// Generated by VIT (Verified Incremental Translator)
// After verification: PreFilterMeasuredSignals kernel PASSED

#include "vit_types.h"

// --- User-provided C++ implementation ---
// VIT Translation
// Function: PreFilterMeasuredSignals
// Source: Filters.f90
// Module: Filters
// Fortran: SUBROUTINE PreFilterMeasuredSignals(CntrPar, LocalVar, DebugVar, objInst, ErrVar)
// Source MD5: 8db86abc7fd4
// VIT: 0.1.0
// Status: unverified

#include "vit_types.h"
#include "rosco_constants.h"
#include <cmath>

// Callee entry points
extern "C" {
    double lpfilter_c(double InputSignal, double DT, double CornerFreq,
                      filterparameters_t* FP, int iStatus, int reset, int* inst,
                      int has_InitialValue, double InitialValue);
    double hpfilter_c(double InputSignal, double DT, double CornerFreq,
                      filterparameters_t* FP, int iStatus, int reset, int* inst,
                      int has_InitialValue, double InitialValue);
    double seclpfilter_c(double InputSignal, double DT, double CornerFreq, double Damp,
                         filterparameters_t* FP, int iStatus, int reset, int* inst,
                         int has_InitialValue, double InitialValue);
    double notchfilter_c(double InputSignal, double DT, double omega,
                         double betaNum, double betaDen,
                         filterparameters_t* FP, int iStatus, int reset, int* inst,
                         int has_InitialValue, double InitialValue);
    double notchfilterslopes_c(double InputSignal, double DT, double CornerFreq, double Damp,
                               filterparameters_t* FP, int iStatus, int reset, int* inst,
                               int has_Moving, int Moving,
                               int has_InitialValue, double InitialValue);
    double wrap_180_c(double x);
}

void PreFilterMeasuredSignals(controlparameters_view_t* CntrPar, localvariables_t* LocalVar,
                              debugvariables_t* DebugVar, objectinstances_t* objInst,
                              errorvariables_t* ErrVar) {
    // If there's an error, don't even try to run
    if (ErrVar->aviFAIL < 0) {
        return;
    }

    int reset = (LocalVar->restart != 0);

    // Filter the HSS (generator) and LSS (rotor) speed measurement:
    // Apply Low-Pass Filter (choice between first- and second-order low-pass filter)
    if (CntrPar->F_LPFType == 1) {
        LocalVar->GenSpeedF = lpfilter_c(LocalVar->GenSpeed, LocalVar->DT,
            CntrPar->F_LPFCornerFreq, &LocalVar->FP, LocalVar->iStatus, reset,
            &objInst->instLPF, 0, 0.0);
        LocalVar->RotSpeedF = lpfilter_c(LocalVar->RotSpeed, LocalVar->DT,
            CntrPar->F_LPFCornerFreq, &LocalVar->FP, LocalVar->iStatus, reset,
            &objInst->instLPF, 0, 0.0);
    } else if (CntrPar->F_LPFType == 2) {
        LocalVar->GenSpeedF = seclpfilter_c(LocalVar->GenSpeed, LocalVar->DT,
            CntrPar->F_LPFCornerFreq, CntrPar->F_LPFDamping, &LocalVar->FP,
            LocalVar->iStatus, reset, &objInst->instSecLPF, 0, 0.0);
        LocalVar->RotSpeedF = seclpfilter_c(LocalVar->RotSpeed, LocalVar->DT,
            CntrPar->F_LPFCornerFreq, CntrPar->F_LPFDamping, &LocalVar->FP,
            LocalVar->iStatus, reset, &objInst->instSecLPF, 0, 0.0);
    }

    // Apply Notch Filter to Gen Speed
    for (int n = 1; n <= CntrPar->F_GenSpdNotch_N; n++) {
        int idx = CntrPar->F_GenSpdNotch_Ind[n - 1] - 1;
        LocalVar->GenSpeedF = notchfilter_c(LocalVar->GenSpeedF, LocalVar->DT,
            CntrPar->F_NotchFreqs[idx], CntrPar->F_NotchBetaNum[idx],
            CntrPar->F_NotchBetaDen[idx], &LocalVar->FP, LocalVar->iStatus, reset,
            &objInst->instNotch, 0, 0.0);
    }

    // Filtering the tower fore-aft acceleration signal
    // Force to start at 0
    if (LocalVar->iStatus == 0 && LocalVar->Time == 0) {
        LocalVar->NacIMU_FA_RAcc = 0;
        LocalVar->FA_Acc_Nac = 0;
    }

    // Low pass
    LocalVar->NACIMU_FA_AccF = seclpfilter_c(LocalVar->NacIMU_FA_RAcc, LocalVar->DT,
        CntrPar->F_FlCornerFreq[0], CntrPar->F_FlCornerFreq[1], &LocalVar->FP,
        LocalVar->iStatus, reset, &objInst->instSecLPF, 0, 0.0);
    LocalVar->FA_AccF = seclpfilter_c(LocalVar->FA_Acc_Nac, LocalVar->DT,
        CntrPar->F_FlCornerFreq[0], CntrPar->F_FlCornerFreq[1], &LocalVar->FP,
        LocalVar->iStatus, reset, &objInst->instSecLPF, 0, 0.0);

    // High pass
    LocalVar->NACIMU_FA_AccF = hpfilter_c(LocalVar->NACIMU_FA_AccF, LocalVar->DT,
        CntrPar->F_FlHighPassFreq, &LocalVar->FP, LocalVar->iStatus, reset,
        &objInst->instHPF, 0, 0.0);
    LocalVar->FA_AccF = hpfilter_c(LocalVar->FA_AccF, LocalVar->DT,
        CntrPar->F_FlHighPassFreq, &LocalVar->FP, LocalVar->iStatus, reset,
        &objInst->instHPF, 0, 0.0);

    // Tower top notch filters
    for (int n = 1; n <= CntrPar->F_TwrTopNotch_N; n++) {
        int idx = CntrPar->F_TwrTopNotch_Ind[n - 1] - 1;
        LocalVar->NACIMU_FA_AccF = notchfilter_c(LocalVar->NACIMU_FA_AccF, LocalVar->DT,
            CntrPar->F_NotchFreqs[idx], CntrPar->F_NotchBetaNum[idx],
            CntrPar->F_NotchBetaDen[idx], &LocalVar->FP, LocalVar->iStatus, reset,
            &objInst->instNotch, 0, 0.0);
        LocalVar->FA_AccF = notchfilter_c(LocalVar->FA_AccF, LocalVar->DT,
            CntrPar->F_NotchFreqs[idx], CntrPar->F_NotchBetaNum[idx],
            CntrPar->F_NotchBetaDen[idx], &LocalVar->FP, LocalVar->iStatus, reset,
            &objInst->instNotch, 0, 0.0);
    }

    // FA acc for ForeAft damping
    if (CntrPar->TD_Mode > 0) {
        LocalVar->FA_AccHPF = hpfilter_c(LocalVar->FA_Acc_Nac, LocalVar->DT,
            CntrPar->FA_HPFCornerFreq, &LocalVar->FP, LocalVar->iStatus, reset,
            &objInst->instHPF, 0, 0.0);
    }

    // Filter Wind Speed Estimator Signal
    LocalVar->WE_Vw_F = lpfilter_c(LocalVar->WE_Vw, LocalVar->DT,
        CntrPar->F_WECornerFreq, &LocalVar->FP, LocalVar->iStatus, reset,
        &objInst->instLPF, 0, 0.0);

    // Blade root bending moment for IPC
    for (int K = 0; K < LocalVar->NumBl; K++) {
        if ((CntrPar->IPC_ControlMode > 0) || (CntrPar->Flp_Mode == 3)) {
            // Moving inverted notch at rotor speed to isolate 1P
            LocalVar->rootMOOPF[K] = notchfilterslopes_c(LocalVar->rootMOOP[K],
                LocalVar->DT, LocalVar->RotSpeedF, 0.7, &LocalVar->FP,
                LocalVar->iStatus, reset, &objInst->instNotchSlopes,
                1, 1,    // has_Moving=1, Moving=1 (.TRUE.)
                0, 0.0); // has_InitialValue=0
        } else if (CntrPar->Flp_Mode == 2) {
            // Filter Blade root bending moments
            LocalVar->rootMOOPF[K] = seclpfilter_c(LocalVar->rootMOOP[K], LocalVar->DT,
                CntrPar->F_FlpCornerFreq[0], CntrPar->F_FlpCornerFreq[1], &LocalVar->FP,
                LocalVar->iStatus, reset, &objInst->instSecLPF, 0, 0.0);
            LocalVar->rootMOOPF[K] = hpfilter_c(LocalVar->rootMOOPF[K], LocalVar->DT,
                0.1, &LocalVar->FP, LocalVar->iStatus, reset,
                &objInst->instHPF, 0, 0.0);

            // Apply gen speed notch filters to blade root signal
            for (int n = 1; n <= CntrPar->F_GenSpdNotch_N; n++) {
                int idx = CntrPar->F_GenSpdNotch_Ind[n - 1] - 1;
                LocalVar->rootMOOPF[K] = notchfilter_c(LocalVar->rootMOOPF[K], LocalVar->DT,
                    CntrPar->F_NotchFreqs[idx], CntrPar->F_NotchBetaNum[idx],
                    CntrPar->F_NotchBetaDen[idx], &LocalVar->FP, LocalVar->iStatus, reset,
                    &objInst->instNotch, 0, 0.0);
            }
        } else {
            LocalVar->rootMOOPF[K] = LocalVar->rootMOOP[K];
        }
    }

    // Control commands (used by WSE, mostly)
    LocalVar->VS_LastGenTrqF = seclpfilter_c(LocalVar->VS_LastGenTrq, LocalVar->DT,
        CntrPar->F_LPFCornerFreq, 0.7, &LocalVar->FP, LocalVar->iStatus, reset,
        &objInst->instSecLPF, 0, 0.0);
    LocalVar->BlPitchCMeasF = seclpfilter_c(LocalVar->BlPitchCMeas, LocalVar->DT,
        CntrPar->F_LPFCornerFreq * 0.25, 0.7, &LocalVar->FP, LocalVar->iStatus, reset,
        &objInst->instSecLPF, 0, 0.0);

    // Wind vane signal
    double NacVaneCosF = lpfilter_c(cos(LocalVar->NacVane * D2R), LocalVar->DT,
        CntrPar->F_YawErr, &LocalVar->FP, LocalVar->iStatus, 0,
        &objInst->instLPF, 0, 0.0);
    double NacVaneSinF = lpfilter_c(sin(LocalVar->NacVane * D2R), LocalVar->DT,
        CntrPar->F_YawErr, &LocalVar->FP, LocalVar->iStatus, 0,
        &objInst->instLPF, 0, 0.0);
    LocalVar->NacVaneF = wrap_180_c(atan2(NacVaneSinF, NacVaneCosF) * R2D);

    // Debug Variables
    DebugVar->GenSpeedF = LocalVar->GenSpeedF;
    DebugVar->RotSpeedF = LocalVar->RotSpeedF;
    DebugVar->NacIMU_FA_AccF = LocalVar->NACIMU_FA_AccF;
    DebugVar->FA_AccF = LocalVar->FA_AccF;
}

// --- Fortran-callable C wrapper ---
extern "C" {
    void prefiltermeasuredsignals_c(controlparameters_view_t* CntrPar, localvariables_t* LocalVar, debugvariables_t* DebugVar, objectinstances_t* objInst, errorvariables_t* ErrVar) {
        PreFilterMeasuredSignals(CntrPar, LocalVar, DebugVar, objInst, ErrVar);
    }
}
