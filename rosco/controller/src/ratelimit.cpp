// C++ implementation of Fortran function: ratelimit
// Generated by VIT (Verified Incremental Translator)
// After verification: ratelimit kernel PASSED

#include "vit_types.h"

// --- User-provided C++ implementation ---
// VIT Translation Scaffold
// Function: ratelimit
// Source: Functions.f90
// Module: Functions
// Fortran: FUNCTION ratelimit(inputSignal, minRate, maxRate, DT, reset, rlP, inst, ResetValue)
// Source MD5: de534d2a6f1f
// VIT: 0.1.0
// Status: unverified
// Generated: 2026-03-18T15:03:43Z

#include "vit_types.h"
#include <algorithm>

extern "C" {
    double saturate_c(double inputValue, double minValue, double maxValue);
}

double ratelimit(double inputSignal, double minRate, double maxRate, double DT, int reset, rlparams_t* rlP, int* inst, int has_ResetValue, double ResetValue) {
    // Determine reset value: use ResetValue if present, else inputSignal
    double resetValue_ = inputSignal;
    if (has_ResetValue) {
        resetValue_ = ResetValue;
    }

    double result;

    // Fortran inst is 1-based; C array is 0-based
    int idx = *inst - 1;

    if (reset) {
        rlP->LastSignal[idx] = resetValue_;
        result = resetValue_;
    } else {
        // Compute unsaturated rate
        double rate = (inputSignal - rlP->LastSignal[idx]) / DT;
        // Saturate the rate — call saturate_c (matching Fortran's function call to saturate)
        rate = saturate_c(rate, minRate, maxRate);

        result = rlP->LastSignal[idx] + rate * DT;

        rlP->LastSignal[idx] = result;
    }

    // Increment instance (Fortran: inst = inst + 1)
    *inst = *inst + 1;

    return result;
}

// --- Fortran-callable C wrapper ---
extern "C" {
    double ratelimit_c(double inputSignal, double minRate, double maxRate, double DT, int reset, rlparams_t* rlP, int* inst, int has_ResetValue, double ResetValue) {
        return ratelimit(inputSignal, minRate, maxRate, DT, reset, rlP, inst, has_ResetValue, ResetValue);
    }
}
