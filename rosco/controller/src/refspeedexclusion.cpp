// C++ implementation of Fortran function: RefSpeedExclusion
// Generated by VIT (Verified Incremental Translator)
// After verification: RefSpeedExclusion kernel PASSED

#include "vit_types.h"

// --- User-provided C++ implementation ---
// VIT Translation Scaffold
// Function: RefSpeedExclusion
// Source: ControllerBlocks.f90
// Module: ControllerBlocks
// Fortran: SUBROUTINE RefSpeedExclusion(LocalVar, CntrPar, objInst, DebugVar)
// Source MD5: 13de1ba11c7e
// VIT: 0.1.0
// Status: unverified
// Generated: 2026-03-28T12:54:51Z

#include "vit_types.h"
extern "C" {

double ratelimit_c(double inputSignal, double minRate, double maxRate, double DT,
                   int reset, rlparams_t* rlP, int* inst,
                   int has_ResetValue, double ResetValue);

void RefSpeedExclusion(localvariables_t* LocalVar, controlparameters_view_t* CntrPar, objectinstances_t* objInst, debugvariables_t* DebugVar) {

    // Get LSS Ref speed
    double VS_RefSpeed_LSS = LocalVar->VS_RefSpd / CntrPar->WE_GearboxRatio;

    if ((VS_RefSpeed_LSS > CntrPar->TRA_ExclSpeed - CntrPar->TRA_ExclBand / 2) &&
        (VS_RefSpeed_LSS < CntrPar->TRA_ExclSpeed + CntrPar->TRA_ExclBand / 2)) {
        LocalVar->FA_Hist = 1;
    } else {
        LocalVar->FA_Hist = 0;
    }

    // Initialize last reference speed state
    if (LocalVar->restart != 0) {
        if (LocalVar->FA_Hist > 0) {
            if (VS_RefSpeed_LSS > CntrPar->TRA_ExclSpeed) {
                LocalVar->TRA_LastRefSpd = CntrPar->TRA_ExclSpeed + CntrPar->TRA_ExclBand / 2;
            } else {
                LocalVar->TRA_LastRefSpd = CntrPar->TRA_ExclSpeed - CntrPar->TRA_ExclBand / 2;
            }
        } else {
            LocalVar->TRA_LastRefSpd = VS_RefSpeed_LSS;
        }
    }

    if (LocalVar->FA_Hist > 0) {
        LocalVar->VS_RefSpd_TRA = LocalVar->TRA_LastRefSpd;
    } else {
        LocalVar->VS_RefSpd_TRA = VS_RefSpeed_LSS;
    }

    LocalVar->TRA_LastRefSpd = LocalVar->VS_RefSpd_TRA;

    // Rate limit reference speed
    LocalVar->VS_RefSpd_RL = ratelimit_c(LocalVar->VS_RefSpd_TRA, -CntrPar->TRA_RateLimit, CntrPar->TRA_RateLimit,
                                          LocalVar->DT, (LocalVar->restart != 0) ? 1 : 0,
                                          &LocalVar->rlP, &objInst->instRL, 0, 0.0);
    LocalVar->VS_RefSpd = LocalVar->VS_RefSpd_RL * CntrPar->WE_GearboxRatio;
}

} // extern "C"

// --- Fortran-callable C wrapper ---
extern "C" {
    void refspeedexclusion_c(localvariables_t* LocalVar, controlparameters_view_t* CntrPar, objectinstances_t* objInst, debugvariables_t* DebugVar) {
        RefSpeedExclusion(LocalVar, CntrPar, objInst, DebugVar);
    }
}
