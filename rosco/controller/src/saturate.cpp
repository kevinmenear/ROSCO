// C++ implementation of Fortran function: saturate
// Generated by VIT (Verified Incremental Translator)
// After verification: saturate kernel PASSED

#include <algorithm>
#include <cmath>

// --- User-provided C++ implementation ---
// VIT Translation
// Function: saturate
// Source: Functions.f90
// Module: Functions
// Fortran: FUNCTION saturate(inputValue, minValue, maxValue)
// Status: verified

#include <algorithm>
double saturate(double val, double minval, double maxval) {
    return std::min(std::max(val, minval), maxval);
}

// --- Fortran-callable C wrapper ---
extern "C" {
    double saturate_c(double inputValue, double minValue, double maxValue) {
        return saturate(inputValue, minValue, maxValue);
    }
}
