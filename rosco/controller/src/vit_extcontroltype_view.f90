! Auto-generated by VIT (Verified Incremental Translator)
! View populator for TYPE(ExtControlType)
! DO NOT EDIT — regenerate with: vit analyze-types --scan-allocatable --type ExtControlType

MODULE vit_extcontroltype_view
    USE ISO_C_BINDING
    USE ROSCO_Types, ONLY: ExtControlType
    IMPLICIT NONE
    PRIVATE
    PUBLIC :: extcontroltype_view_t, vit_populate_extcontroltype, vit_copy_scalars_to_extcontroltype, vit_original_extcontroltype

    ! Kernel stash: pointer to original Fortran type, set by wrapper before calling C++.
    ! Callee bridges USE this to pass the original type to Fortran callees.
    TYPE(ExtControlType), POINTER, SAVE :: vit_original_extcontroltype => NULL()

    ! Mirror of the C view struct — layout MUST match extcontroltype_view_t in C header
    TYPE, BIND(C) :: extcontroltype_view_t
        ! ALLOCATABLE fields (pointer + size)
        TYPE(C_PTR) :: avrSWAP = C_NULL_PTR
        INTEGER(C_INT32_T) :: n_avrSWAP = 0
    END TYPE extcontroltype_view_t

CONTAINS

    SUBROUTINE vit_populate_extcontroltype(src, view)
        TYPE(ExtControlType), INTENT(IN), TARGET :: src
        TYPE(extcontroltype_view_t), INTENT(OUT) :: view

        ! --- Scalars ---

        ! --- ALLOCATABLE arrays ---
        IF (ALLOCATED(src%avrSWAP)) THEN
            view%avrSWAP = C_LOC(src%avrSWAP(1))
            view%n_avrSWAP = INT(SIZE(src%avrSWAP), C_INT32_T)
        ELSE
            view%avrSWAP = C_NULL_PTR
            view%n_avrSWAP = 0
        END IF

    END SUBROUTINE vit_populate_extcontroltype

    SUBROUTINE vit_copy_scalars_to_extcontroltype(view, dest)
        TYPE(extcontroltype_view_t), INTENT(IN) :: view
        TYPE(ExtControlType), INTENT(INOUT) :: dest

        ! --- Scalars (reverse of populator) ---

    END SUBROUTINE vit_copy_scalars_to_extcontroltype

END MODULE vit_extcontroltype_view
